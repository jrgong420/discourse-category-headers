show_category_name:
  type: bool
  default: true
  description: "Show the category name in the header"

show_category_description:
  type: bool
  default: true
  description: 'Show the category description text<br>(The first paragraph of the "About this category" topic)'

show_full_category_description:
  type: bool
  default: false
  description: 'Show the full category description text<br>(The full text of the "About this category" topic)'

title_text_size:
  type: enum
  default: normal
  choices:
    - smallest
    - smaller
    - normal
    - larger
    - largest
  description: "Size of the category title (H1)"

description_text_size:
  type: enum
  default: larger
  choices:
    - smallest
    - smaller
    - normal
    - larger
    - largest
  description: "Size of text within the category description"

text_align:
  type: enum
  default: left
  choices:
    - left
    - center
    - right
  description: "Alignment of the text within the category header"

show_subcategory_header:
  type: bool
  default: true
  description: "Show header for subcategories"

show_parent_category_name:
  type: bool
  default: true
  description: "Prefix the parent category name on the subcategory headers"

show_lock_icon:
  type: bool
  default: true
  description: "Show the lock icon on categories protected by permissions"

category_lock_icon:
  type: string
  default: ""
  description: "Enter the Font Awesome 5 icon name to display instead of the lock icon next to private categories"

show_category_logo:
  type: bool
  default: true
  description: "Show the category logo image within the header"

show_dark_mode_category_logo:
  type: bool
  default: false
  description: "Show the dark mode category logo instead of the normal category logo."

show_parent_category_logo:
  type: bool
  default: true
  description: "Show the parent category logo when a subcategory logo is not set"

show_dark_mode_parent_category_logo:
  type: bool
  default: false
  description: "Show the parent category's dark mode logo when a subcategory logo is not set."

show_site_logo:
  type: bool
  default: true
  description: "Show the small site logo if a category logo is not set"

position_logo:
  type: enum
  default: left
  choices:
    - left
    - right
    - top
  description: "Position of the logo within the header. Left and right display the logo inline with the text<br>Top displays the logo above aligned with the text"

size_logo:
  type: enum
  default: standard
  choices:
    - standard
    - small
    - original
  description:
    "Size of the logo in the header.<br>Small is ~40px high (similar to subcategory box logo)<br>Standard is ~150px high<br>Original is the uploaded image size"

header_style:
  type: enum
  default: box
  choices:
    - box
    - banner
    - none
  description: "Set the header style:<br>Box: bordered container with category color as left border<br>Banner: filled background using category color and text color<br>None: no special styling"

header_background_image:
  type: enum
  default: contain
  choices:
    - contain
    - cover
    - resize
    - outside
  description: "Applies if you have uploaded a category background image<br>Contain, cover and resize display the background within the header<br>Outside is the Discourse default displaying it outside the header."

show_parent_category_background_image:
  type: bool
  default: false
  description: "Show the background image of the parent category instead. Follows the setting in `header_background_image`."

show_mobile:
  type: bool
  default: true
  description: "Show category header on mobile devices"

force_mobile_alignment:
  type: bool
  default: false
  description: "Force mobile alignment of logo-text to the top-centre of the header (when false, maintains left-right layout on mobile)"

hide_if_no_category_description:
  type: bool
  default: true
  description: "Hide header if category description is not set"

hide_category_exceptions:
  type: list
  default: ""
  list_type: category
  description: "Headers will not show for these categories"

show_read_more_link:
  type: bool
  default: true
  description: 'Show the "Read more" link at the bottom of the category header'

inline_read_more:
  type: bool
  default: false
  description: 'Put "Read more" link at the end of the category description'

expand_and_collapse_category_description:
  type: bool
  default: false
  description: "Expand/collapse the category description when the Read more/Read less links are clicked"

read_more_link_text:
  type: string
  default: "Read more about this category…"
  description: 'Custom text for the "Read more" link'

read_less_link_text:
  type: string
  default: "Read less…"
  description: 'Custom text for the "Read less" link'

show_category_follow_button:
  type: bool
  default: true
  description: "Show the category follow/notification button inside the custom category header"


# UI control for category description toggling
category_description_toggle_ui:
  type: enum
  default: "read_more_only"
  choices:
    - chevron_only
    - read_more_only
    - both
    - none
  description: "Choose how users can expand/collapse category descriptions: chevron icon, read-more link, both, or none"
